# OpenClaw Chat 功能操作说明手册

本文档面向管理员和普通用户，说明 OpenClaw Chat 的初始化、登录、聊天、会话历史、用户管理和常见问题处理。

## 1. 角色说明

### 管理员

管理员可以：

- 初始化租户。
- 登录聊天页面。
- 查看全部 Agent 的会话列表和历史。
- 创建新会话。
- 邀请普通用户。
- 查看用户列表、复制激活链接、删除普通用户。
- 临时查看和调整网关配置字段。

### 普通用户

普通用户可以：

- 登录聊天页面。
- 自动连接自己的 OpenClaw Agent。
- 查看自己 Agent 的主会话和历史会话。
- 创建自己的新会话。
- 使用日期和关键词筛选自己的会话。

普通用户不能：

- 查看其它 Agent 的会话。
- 切换到其它 Agent 的历史。
- 管理用户。
- 修改网关地址、Token、Origin。

## 2. 首次初始化

打开：

```text
/setup
```

填写以下信息：

- 租户名称：用于区分一组用户和一台 OpenClaw Gateway。
- 网关地址：OpenClaw Gateway WebSocket 地址，例如 `ws://host:port/ws`。
- 令牌：OpenClaw Gateway Token。
- 来源域：Gateway 校验 Origin 时使用，可选。
- 管理员用户名：管理员对应的 OpenClaw agent 名。
- 管理员邮箱：登录账号。
- 管理员密码：至少 6 位。

提交后系统会：

- 校验 Gateway 能否连接。
- 创建租户。
- 创建管理员账号。
- 保存租户和用户到 SQLite。

初始化完成后，进入 `/login` 登录。

## 3. 登录与自动连接

打开：

```text
/login
```

使用邮箱和密码登录。

登录成功后自动进入：

```text
/chat
```

进入聊天页面后系统会自动：

- 连接 OpenClaw Gateway。
- 切换到当前用户 Agent 的主会话。
- 加载会话列表。
- 加载当前会话最近 200 条历史。
- 将页面滚动到最新消息。
- 聚焦聊天输入框。

默认主会话格式：

```text
agent:{AgentName}:main
```

例如：

```text
agent:main:main
agent:qq:main
```

## 4. 聊天操作

### 发送消息

在底部输入框输入问题。

- `Enter`：提交消息。
- `Shift + Enter`：换行。

发送后页面会自动滚动到最新提问和回复。

### 停止回复

如果回复正在生成，可以使用停止按钮终止当前回复。底层调用后端：

```text
chat.abort
```

### 图片附件

可以上传图片附件，发送时会和文本一起提交到后端。

### 复制回复

助手回复旁的“复制”会把回复文本复制到输入框，方便继续编辑或追问。

## 5. 流式回复

当前版本的助手正文默认使用 OpenClaw 后端流式事件。

主要事件来源：

- `agent` 事件：接收 assistant 增量正文。
- `chat` 事件：接收 delta、final、aborted、error 状态。

如果短时间内没有收到正文流式内容，前端才会通过 `chat.history` 做兜底查询；已开始流式输出时不会再让历史查询抢渲染，避免重复段落。

回复结束后，输入焦点会自动回到聊天输入框。

## 6. 思考与工具流显示

配置区提供两个独立选项：

- 显示思考
- 显示工具流

关闭“显示思考”后，助手消息中的 thinking 内容不会显示。

关闭“显示工具流”后，工具调用、工具结果、压缩、fallback 等过程信息不会插入聊天流。

这些选项只影响前端显示，不会删除后端历史。

## 7. Markdown 与表格

助手回复支持 Markdown 渲染，包括：

- 标题
- 列表
- 代码块
- 行内代码
- 表格
- 引用

表格必须是标准 Markdown 表格，例如：

```markdown
| 文件 | 用途 | 行数 |
|------|------|------|
| AIBridgeService.cs | 核心桥接服务 | 552 |
| README.md | 接入指南 | 93 |
```

如果分隔行列数不完整，Markdown 解析器可能不会把它识别为表格。

## 8. 会话标识与历史

### 主会话

每个用户登录后默认进入自己的主会话：

```text
agent:{AgentName}:main
```

主会话适合作为长期默认对话。

### 新会话

点击“新会话”不会发送 `/new`，也不会清空后端历史。

它会创建一个新的日期会话标识：

```text
agent:{AgentName}:yyyyMMdd-HHmmss-xxxxxx
```

例如：

```text
agent:qq:20260428-164530-a1b2c3
```

这样做的目的：

- 保留主会话历史。
- 让新主题独立保存。
- 方便后续按日期查找。

### 查看历史

配置区的“会话标识”提供：

- 日期筛选。
- 关键词搜索。
- 会话下拉。

选择某个会话后，页面会调用：

```text
chat.history
```

加载该会话最近 200 条历史。

### 管理员和普通用户的区别

管理员：

- 能看到全部 Agent 的会话。
- 可切换任意会话查看历史。
- 默认下拉只显示最近 30 个，避免列表过长。

普通用户：

- 只能看到自己的 Agent 会话。
- 不能切换到其它 Agent。
- 默认下拉也只显示最近 30 个。

## 9. 为什么取消了连接、断开、刷新会话按钮

当前设计把聊天页面作为一个“登录即用”的工具：

- 登录后自动连接。
- 离开或注销时断开。
- 会话列表在登录和新会话时自动加载。

因此主界面不再保留：

- 连接
- 断开
- 刷新会话

如果其它客户端新增了会话，当前页面需要重新进入或后续增加更隐蔽的刷新入口。

## 10. 用户管理

管理员可以点击“用户管理”。

### 邀请用户

填写：

- 用户名：同时作为 OpenClaw agent 名。
- 邮箱：用户登录账号，全局唯一。

提交后系统生成：

- 激活链接。
- 后端 agent 创建命令。

示例命令：

```text
openclaw agents add qq --workspace ~/.openclaw/workspace-qq
```

管理员需要在 OpenClaw 后端执行该命令，确保后端存在对应 agent。

### 激活用户

把激活链接发给用户。

用户打开链接后设置密码，激活完成即可登录。

### 删除用户

管理员可以删除普通用户。

删除前端用户不会自动删除 OpenClaw 后端 agent。如果要彻底移除，需要在后端单独处理 agent 和 workspace。

## 11. 数据保存位置

### 前端用户和租户

保存在 SQLite：

```text
openclaw-chat.db
```

默认位于应用输出目录。

可通过 `UserStore:DatabasePath` 修改。

### OpenClaw 聊天历史

聊天历史由 OpenClaw 后端保存。前端只是通过 `chat.history` 读取，不自行持久化聊天内容。

后端常见路径类似：

```text
/home/node/.openclaw/agents/{agent}/sessions/sessions.json
```

具体以 OpenClaw 部署配置为准。

### Data Protection 密钥

Blazor Server 相关密钥保存在：

```text
.data-protection-keys/
```

不要随意删除，否则可能导致已登录状态失效。

## 12. 常见问题

### 登录后没有显示历史

检查会话标识是否为正确主会话：

```text
agent:{AgentName}:main
```

如果显示的是日期会话，说明当前选择的是某个新会话，只会加载那个会话的历史。

### 输入文字后历史才出现

这通常是前端渲染没有及时刷新。当前版本已在自动连接和初始历史加载完成后主动刷新组件。

### 表格显示不像表格

确认 Markdown 表格分隔行完整：

```markdown
| A | B | C |
|---|---|---|
| 1 | 2 | 3 |
```

不要只有：

```markdown
| A | B | C |
|---|
```

### 新会话会不会清空历史

不会。

当前“新会话”不会发送 `/new`，而是切换到新的 sessionKey。旧 sessionKey 的历史仍在后端。

### 手动发送 `/new` 会怎样

如果在输入框里手动发送 `/new`，后端可能会重置当前会话或创建新会话，具体取决于 OpenClaw 后端实现。为了保留历史，建议使用页面的“新会话”按钮。

### 管理员看到会话太多怎么办

使用日期筛选或关键词搜索。页面默认只显示最近 30 个匹配项。

### 普通用户看不到别人的历史

这是权限设计。普通用户只能看到自己 Agent 下的会话。

### Edge 一直转圈，Chrome 正常

通常和 Data Protection 密钥写入权限有关。当前项目已改为写入 `.data-protection-keys/`。确认应用进程对项目目录有写权限，并重启应用。

## 13. 运维建议

- 定期备份 `openclaw-chat.db`。
- 定期备份 OpenClaw 后端 `.openclaw/agents/*/sessions`。
- 不要把生产 Token 提交到 Git。
- 若部署到公网，务必使用 HTTPS。
- Gateway 的 allowed origins 应只允许可信域名。
